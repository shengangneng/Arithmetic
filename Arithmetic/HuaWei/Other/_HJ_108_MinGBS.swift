//
//  _HJ_108_MinGBS.swift
//  Arithmetic
//
//  Created by shengangneng on 2023/9/22.
//

import Foundation

struct _HJ_108_MinGBS {
    /*
     
     while let line = readLine() {
         let parts = line.split(separator: " ")
         let numA = Int(parts[0])!
         let numB = Int(parts[1])!
         print(numA * numB / getGCD(numA, numB))
     }

     // 先求出最大公倍数【比如5 7 = 1，2 4 = 2】
     private func getGCD(_ m: Int, _ n: Int) -> Int {
         let maxVal = max(m, n)
         let minVal = min(m, n)
         if maxVal % minVal == 0 {
             return minVal
         } else {
             return getGCD(maxVal % minVal, minVal)
         }
     }
     
     */
}
