//
//  _HJ_27_FindBrotherWord.swift
//  Arithmetic
//
//  Created by shengangneng on 2023/9/21.
//

import Foundation

// https://www.nowcoder.com/practice/03ba8aeeef73400ca7a37a5f3370fe68?tpId=37&tqId=21250&rp=1&ru=/exam/oj/ta&qru=/exam/oj/ta&sourceUrl=%2Fexam%2Foj%2Fta%3FtpId%3D37&difficulty=undefined&judgeStatus=undefined&tags=&title=
struct _HJ_27_FindBrotherWord {
    /*
     
     while let string = readLine() {
         var array = string.components(separatedBy: " ")
         let n = array[0]
         array.removeFirst()
         let k: Int! = Int(array.last!)
         array.removeLast()
         let target: String! = array.last
         array.removeLast()

         var res = [String]()
         for str in array {
             if str == target || str.count != target.count {
                 continue
             }
             if str.sorted() == target.sorted() {
                 res.append(str)
             }
         }
         print(res.count)
         res = res.sorted()
         if k <= res.count {
             print(res[k - 1])
         }

     }
     
     */
}
