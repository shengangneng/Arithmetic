//
//  _HJ_68_SortScore.swift
//  Arithmetic
//
//  Created by shengangneng on 2023/9/21.
//

import Foundation

// https://www.nowcoder.com/practice/8e400fd9905747e4acc2aeed7240978b?tpId=37&tqId=21291&rp=1&ru=/exam/oj/ta&qru=/exam/oj/ta&sourceUrl=%2Fexam%2Foj%2Fta%3FtpId%3D37&difficulty=undefined&judgeStatus=undefined&tags=&title=
struct _HJ_68_SortScore {
    func sortScore() {

        /*
         
         struct Person {
             var name: String?
             var score: Int?
         }

         while let count = readLine() {
             let type = readLine()
             var persons = [Person]()
             while let str = readLine() {
                 var arr = str.components(separatedBy: " ")
                 let person = Person(name: arr[0], score: Int(arr[1]))
                 persons.append(person)
             }
             persons = persons.sorted { v1, v2 in
                 if type == "0" {
                     return v1.score! > v2.score!
                 } else {
                     return v1.score! < v2.score!
                 }
             }
             for person in persons {
                 print("\(person.name!) \(person.score!)")
             }
         }
         
         */


    }
}
