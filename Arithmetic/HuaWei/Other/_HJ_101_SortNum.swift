//
//  _HJ_101_SortNum.swift
//  Arithmetic
//
//  Created by shengangneng on 2023/9/21.
//

import Foundation

struct _HJ_101_SortNum {
    /*
     
     while let count = readLine() {
         var numStr: String! = readLine()
         let nums = (numStr as String).components(separatedBy: " ")
         let sort = readLine()
         let res = nums.compactMap { Int($0) }.sorted { v1, v2 in
             if Int(sort!) == 0 {
                 return v1 < v2
             } else {
                 return v1 > v2
             }
         }
         for num in res {
             print(num, terminator: " ")
         }
     }
     
     
     */
}
